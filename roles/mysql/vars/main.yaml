#MYSQL_ROOT_PASSWORD: RoboShop@1
#mysql_secret: "{{ lookup('amazon.aws.secretsmanager_secret', 'roboshop/dev/mysql_root_password', region='us-east-1') | from_json }}"
mysql_secret: "{{ lookup('amazon.aws.aws_ssm', '/roboshop/dev/mysql_root_password', region='us-east-1', decrypt=True ) }}"